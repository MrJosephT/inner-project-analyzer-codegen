Add constant information generated by its name and type to the constant pool and return the index of the constant.
1. Extract name and type information from user input.
2. Add extracted information to constant pool.
3. Return index of the constant in the pool.
SymbolTable.java int setMajorVersionAndClassName(final int majorVersion, final String className)
SymbolTable.java void putConstantPool(final ByteVector output)
SymbolTable.java int hash(final int tag, final String value1, final int value2)
SymbolTable.java int addMergedType(final int typeTableIndex1, final int typeTableIndex2)
SymbolTable.java int hash(final int tag, final String value1, final String value2, final String value3, final int value4)
SymbolTable.java
Symbol.java
Function Header: int addConstantToPool(final String name, final String type)

Description:
Add constant information generated by its name and type to the constant pool and return the index of the constant.

Reuse Functions or Entities:
SymbolTable.java int setMajorVersionAndClassName(final int majorVersion, final String className)
SymbolTable.java void putConstantPool(final ByteVector output)

Parameters:
name: The name of the constant to be added.
type: The type of the constant to be added.

Return Value:
Returns the index of the constant in the pool.
```java
public int addConstantToPool(final String name, final String type) {
    // code to add constant information to the constant pool
    // and return the index of the constant
}
```----------------------------------------
```java
public int addConstantToPool(final String name, final String type) {
    Constant constant = new Constant(name, type);
    constantPool.add(constant);
    
    return constantPool.indexOf(constant);
}
```